name: sbpos_v2
description: A new Flutter project.
version: 1.0.0+1
publish_to: none
environment:
  sdk: '>=3.4.1 <4.0.0'
dependencies:
  core:
    path: core
  dashboard:
    path: features/dashboard
  flutter:
    sdk: flutter
  go_router: ^15.1.2
  landing_page_menu:
    path: features/landing_page_menu
  notification:
    path: features/notification
  outlet:
    path: features/outlet
  product:
    path: features/product
  setting:
    path: features/setting
  transaction:
    path: features/transaction
  expense:
    path: features/expense

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
  integration_test:
    sdk: flutter
  network_image_mock: ^2.1.1
  sqflite_common_ffi: ^2.3.3

flutter:
  uses-material-design: true
  assets:
    - .env.local
    - .env.prod
    - assets/images/
