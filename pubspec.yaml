name: sbpos_v2
description: A new Flutter project.
version: 1.0.0+1
publish_to: none
environment:
  sdk: '>=3.4.1 <4.0.0'
dependencies:
  core:
    path: core
  dashboard:
    path: features/dashboard
  flutter:
    sdk: flutter
  landing_page_menu:
    path: features/landing_page_menu
  mode:
    path: features/mode
  warehouse:
    path: features/warehouse
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
flutter:
  uses-material-design: true  # <-- WAJIB ADA!
